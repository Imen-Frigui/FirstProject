<h1>La liste des formations </h1>
<h3>Le nombre de formation est {{f|length }}  </h3>
{% set maxV = 0 %}
{% for j in f %}
    {% set maxV= max(j.nb_participants, maxV) %}
  {% endfor %}
<h3>Le nombre de Maximale de nb_participants est {{maxV}} </h3>

<table border="2">
    <tr>
        <th>Référence </th>
        <th>Titre </th>
        <th>Description </th>
        <th> NombreParticipants </th>
    </tr>
    {% for i in f %}
   {# {% if i.nb_participants == maxV %}}
    {#{% if 'theorique' in i.Description %}#}
  <tr>
    <td> {{i.ref}}</td>
    <td> {{i.Titre | upper}}</td>
    <td> {{i.Description}}</td>
    <td> {{i.nb_participants}}</td>
  </tr>
  {#{% endif %}#}
  {% endfor %}
</table>
{# MAX
{% set maxV = 0 %}
{% for j in f %}
    {% set maxV= max(j.nb_participants, maxV) %}
  {% endfor %}
#}
{% set somme = 0 %}
 {% for t in f %}
    {% set somme = somme + t.nb_participants %}
 {% endfor %}
<h3>Le somme totale de participant {{somme}}</h3>