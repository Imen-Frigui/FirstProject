<table border="2">
    <tr>
        <th>id </th>
        <th>name </th>
        <th>acrion </th>
    </tr>
    {% for i in c %}
  <tr>
    <td> {{i.id}}</td>
    <td> {{i.name}}</td>
    <td><form action="{{ path('delete', {'id':i.id}) }}">
      <button>DELETE</button></form></td>
  </tr>
  {% endfor %}
</table>